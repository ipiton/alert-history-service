apiVersion: v1
kind: ServiceAccount
metadata:
  name: alert-history-publishing
  namespace: production
  labels:
    app: alert-history
    component: publishing
    environment: production
    managed-by: TN-050
  annotations:
    description: "Production ServiceAccount with token projection"
    owner: "platform-team"
    compliance: "CIS-5.1.5,CIS-5.1.6,PCI-DSS-7.1,PCI-DSS-8.2,SOC2-CC6.2"
    security-hardened: "true"
automountServiceAccountToken: false  # Use token projection instead
